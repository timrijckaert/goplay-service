name: Pipeline

on: [push]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Inject credentials
        run: echo "$CREDENTIALS" >> local.properties
        shell: bash
        env:
         CREDENTIALS : ${{ secrets.CREDENTIALS }} 
  VRT NU:
    needs: [setup]
    runs-on: ubuntu-latest
    steps:
      - name: E2E VRT NU
        run: ./gradlew :vrtnu:test
  VTM GO:
    needs: [setup]
    runs-on: ubuntu-latest
    steps:
      - name: E2E VTM GO
        run: ./gradlew :vtmgo:test
  VIER:
    needs: [setup]
    runs-on: ubuntu-latest
    steps:
      - name: E2E Vier
        run: ./gradlew :vier:test
  Upload:
    needs: [VRT NU, VTM GO, VIER]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/upload-artifact@v2
        with:
          name: Package
          path: build/libs
